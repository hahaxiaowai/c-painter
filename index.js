import { initCanvas } from './dom';
class Painter{
  constructor(elementId){
    // 初始化Dom元素
    const res = initCanvas(elementId);
    // 初始化变量池
    // initVariable()
    // 初始化事件
    // initEvent();
  }
}